---
"vue-lynx": minor
---

Vapor IFR × Element Templates (milestones 1–3): bake straight-line dense/sparse `CLONE_TREE` creators; build-time structured `template(<TemplateNode>)` rewrite; IFR main-thread one-shot `renderEffect`; IFR static root-only + sparse nav facades (skip ShadowElement off the hole path); sparse first-frame naming with paint-registry densify remapping onto BG dense ownership.
