# Options API Example

Tests Vue's Options API support in vue-lynx.

## Features Exercised

- `App.vue` — `data`, `computed`, `methods`, `watch`, `mounted`, `components` option
- `Counter.vue` — `props`, `emits`, `data`, `methods`, `watch`, `computed`
