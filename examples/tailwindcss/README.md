# Tailwind CSS Example

Demonstrates [Tailwind CSS](https://tailwindcss.com/) integration with vue-lynx via `@rsbuild/plugin-sass` and Tailwind's utility classes.

Features a settings/profile UI with runtime theme switching via CSS variables.
