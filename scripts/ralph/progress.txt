# Ralph Progress Log
Started: Sat Mar 21 18:26:48 EDT 2026
---

## Completed Stories (15/15)

### pickergroup-toolbar (priority 95) ✓
- Added showToolbar, confirmButtonText, cancelButtonText, nextStepText props
- Added v-model:activeTab support with confirm/cancel events
- Added toolbar, title, confirm, cancel slots
- nextStepText switches confirm button to advance tabs until final tab

### timepicker-complete (priority 90) ✓
- Added filter prop: (type, values) => string[]
- Added formatter prop: (type, value) => string
- Added visibleOptionNum prop
- Added toolbar, title, confirm, cancel, option, columns-top, columns-bottom slots

### datepicker-complete (priority 89) ✓
- Same pattern as TimePicker: filter, formatter, visibleOptionNum props
- Added all 7 slots (toolbar, title, confirm, cancel, option, columns-top, columns-bottom)

### calendar-popup (priority 88) ✓
- Added show prop with v-model:show support
- Added round, position props
- Wraps in Popup component when poppable=true
- Added update:show, clickOverlay, opened, closed events
- Added title, subtitle, footer slots

### calendar-formatter (priority 85) ✓
- Added formatter prop: (day: CalendarDay) => CalendarDay
- CalendarDay type: { date, type, text, topInfo, bottomInfo, className }
- Formatter applied to each day cell with topInfo/bottomInfo rendering
- Day types: selected, start, end, start-end, middle, disabled, normal, today

### actionsheet-close (priority 82) ✓
- Added closeable prop with closeIcon prop (default 'cross')
- Added safeAreaInsetBottom prop (34px bottom padding)
- Renders close icon in header when closeable=true

### form-auto-required (priority 80) ✓
- Field component: required prop now accepts boolean | 'auto'
- Added rules prop to Field for auto-detection
- When 'auto', checks if any rule has required: true
- Shows asterisk on label when required

### treeselect-badge (priority 78) ✓
- Nav items render Badge component when dot or badge property is set
- Uses Badge component from ../Badge/index.vue
- Matches Vant's TreeSelectItem type (dot, badge properties)

### couponlist-complete (priority 75) ✓
- code prop with v-model support already existed
- Added emptyImage prop (renders image in empty state)
- closeButtonText, inputPlaceholder props already existed
- Added displayedCouponIndex prop (highlights specified coupon)
- update:code event already existed

### pagination-slot (priority 70) ✓
- Added pageDesc slot in simple mode
- Slot receives { current, total } props
- Default: '{current}/{total}' text

### rate-iconprefix (priority 68) ✓
- Added iconPrefix prop
- When iconPrefix set, uses Icon component with class-prefix
- Falls back to Unicode star characters when no iconPrefix

### uploader-callbacks (priority 65) ✓
- Added afterRead, beforeRead, beforeDelete callback props
- beforeDelete called before removing file; returns false to cancel
- Props typed with proper signatures

### tabs-animated (priority 60) ✓
- Added animated prop (default false)
- Provided to child Tab components via TabsProvide
- swipeThreshold prop already existed

### picker-methods (priority 55) ✓
- Exposed confirm() method via defineExpose
- Exposed getSelectedOptions() method via defineExpose

### dialog-methods (priority 54) ✓
- Exposed open() method via defineExpose
- Exposed close() method via defineExpose

---
Build verification: pnpm build ✓, examples/vant-lynx pnpm build ✓

## Quality Review Fixes (2026-03-21)

### PickerGroup confirm event fix
- confirm event was emitting `[]` instead of collecting child picker values
- Added registerPicker/unregisterPicker to provide; Picker now registers its getValues
- confirm now calls collectValues() to gather selected values from all child pickers

### Calendar double update:show fix
- onClickOverlay was emitting update:show manually, but Popup already handles it via doClose()
- Removed duplicate emission; Popup's forwarded @update:show handles closing

### Uploader afterRead fix
- afterRead prop was declared but never invoked
- Added watcher on modelValue that detects newly added files and calls afterRead

### Tab animated transition fix
- animated prop was provided by Tabs parent but never consumed by Tab child
- Tab now injects animated ref and applies opacity transition when enabled

Build verification: pnpm build ✓, examples/vant-lynx pnpm build ✓
