# Ralph Progress Log — Vant Lynx 100% Feature Parity
Started: 2026-03-21
Branch: feat/vant-lynx-v2
---

## Codebase Patterns

- **Test runner**: `pnpm test -- --reporter=verbose Icon` (vitest, no `--testPathPattern`)
- **Test pattern**: Use `defineComponent` + `h()` render, then `container.querySelectorAll` for DOM assertions
- **addUnit helper**: Vant uses `addUnit(size)` to convert numbers to `${n}px` — replicate locally in components
- **Props defaults**: Only set defaults that Vant explicitly sets (e.g. `tag: 'i'`, `classPrefix: 'van-icon'`). Leave size/color unset for inherit behavior.
- **Pre-existing TS errors**: `vue-lynx-testing-library` has no type declarations — this is project-wide, not per-component
- **Style values**: Use string values with units (e.g. `"20px"`) not numbers in inline styles — numeric values trigger deprecation warnings and `getAttribute('style')` may not work on `<text>` elements with numeric values
- **Text element queries**: `querySelector('text')` returns first `<text>` including template whitespace nodes — use `querySelectorAll('text')` and filter by style/content

---

## 2026-03-22 - parity-icon: Icon
- Props: 8/8 (name, size, color, dot, badge, badgeProps, classPrefix, tag)
- Events: 1/1 (click)
- Slots: 1/1 (default)
- CSS Variables: 2 defined in index.less (--van-icon-font-family, --van-icon-font-size)
- Animations: none (Icon has no transitions)
- Tests: 9 cases (Vant has 9) — all pass
- Demo sections: 7 (Basic, URL, Badge, Color, Size, Outlined, Filled) — Vant has 5 demo + 3 icon tabs
- Visual match: pass (unicode fallback icons render correctly)
- Limitations: tag prop no-op, classPrefix no-op, no @font-face, no ConfigProvider iconPrefix
---

## 2026-03-22 - parity-loading: Loading
- Props: 6/6 (type, color, size, textSize, textColor, vertical)
- Events: 0/0 (none in Vant)
- Slots: 2/2 (default, icon)
- CSS Variables: 5 defined in index.less (--van-loading-text-color, --van-loading-text-font-size, --van-loading-spinner-color, --van-loading-spinner-size, --van-loading-spinner-duration)
- Animations: none (Lynx has no SVG or @keyframes support for spinner animations)
- Tests: 9 cases (Vant has 5) — all pass
- Demo sections: 7/7 (Type, Color, Size, Text, Vertical, Text Color, Custom Icon)
- Visual match: pass (border-based spinner approximation)
- Limitations: no SVG (circular uses CSS border), no @keyframes (static spinners), no ::before (spinner uses dotted border)
---

## 2026-03-22 - parity-badge: Badge
- Props: 8/8 (dot, max, tag, color, offset, content, showZero, position)
- Events: 0/0 (none in Vant)
- Slots: 2/2 (default, content)
- CSS Variables: 10 defined in index.less (--van-badge-size, --van-badge-color, --van-badge-padding, --van-badge-font-size, --van-badge-font-weight, --van-badge-border-width, --van-badge-background, --van-badge-dot-color, --van-badge-dot-size, --van-badge-font)
- Animations: none (Badge has no transitions)
- Tests: 12 cases (Vant has 12) — all pass
- Demo sections: 6/6 (Basic Usage, Max, Custom Color, Custom Content, Custom Position, Standalone)
- Visual match: pass (inline styles match Vant layout)
- Limitations: tag prop no-op (Lynx always uses view), CSS class-based styling not available (uses inline styles), transform: translate() not reliable, --van-badge-font not applied
---
