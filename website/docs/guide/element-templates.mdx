# Element Templates

Element Templates are the VDOM-side companion to Instant First-Frame
Rendering: they lower eligible static subtrees so the sync main-thread
create path stays cheap.

See [IFR on VDOM → Element Templates](/guide/ifr#element-templates) for the
compiler model, eligibility rules, semantics, and configuration.
