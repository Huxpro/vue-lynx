# Unified Benchmark Matrix

One place to **look at** every architecture on the same host and ladder —
IFR styles, VDOM vs Vapor, and React vs Vue — without mixing metric scales.

**How to read storms:** **Select = point update** (selection / a few classes
per tick). **Update = batch update** (many rows touched each pass). Don’t
compare those two curves as if they were the same workload.

<BenchArtifactFrame
  src="/benchmark/unified.html"
  srcZh="/benchmark/unified.zh.html"
  title="Unified benchmark matrix"
  titleZh="统一基准矩阵"
  height={2600}
/>

## What this page is

The three older campaigns answered different questions:

| campaign | question | doc |
|---|---|---|
| IFR styles | first-frame cost & FCP | [IFR Benchmarks](/guide/ifr-benchmarks) |
| Framework (VDOM vs Vapor) | instrumented BG/e2e + interactive | [Framework bench](/guide/benchmark-vapor) |

They shared **1k→30k labels** but not a **metric scale**. This report puts
every cell in one schema and leads with decision-first conclusions
(takeaway → so what → verify).

Sibling pages keep only what this matrix does **not** cover:

| page | keeps |
|---|---|
| [Framework bench](/guide/benchmark-vapor) | interactive apps (React as reference), instrumented BG/e2e, ops bytes, bundle/heap |
| [IFR Benchmarks](/guide/ifr-benchmarks) | strategy ladder, examples gzip/TTI, native |

## How to read it

1. **Conclusions** — digestible product guidance; numbers are for checking.
2. **First screen** — the interaction-free half: `startup` (attach → first
   content, empty app) and the `mount-create` ladder (the app is *built*
   with N rows already in the table, 1k→30k). Two curves: absolute ms, and
   the same data normalized to the `vdom` baseline so host drift cancels.
   This is where the third framework family,
   [Octane](https://github.com/octanejs/octane), appears — see below.
3. **Storm table / charts** — point (select) vs batch (update) latency.
   Octane is a full column there, and this is where its result is most
   lopsided — see below.
4. **FCP table / charts** — content-probe first-frame ladder (separate instrument!).
5. **Optimization-flag matrix** — every legal combination in V4 notation
   `render [+b[:d|c|e]] [+ifr[:c|e]]` (baseline = per-node; `+b` = block
   templates; `+b:e` = engine staging, N/A on web and omitted; `+ifr:e` =
   engine-painted first frame only), measured for create/update with
   per-flag marginal factor deltas and a full flag legend on the page.
   Formalization: [`GRAPH-ENG-REPORT.md`](https://github.com/Huxpro/vue-lynx/blob/vapor/packages/ifr-bench/GRAPH-ENG-REPORT.md) §1.3c.

## Octane (third framework family)

[octanejs/octane](https://github.com/octanejs/octane) ships its own private
Lynx renderer (`@octanejs/lynx`), measured here across the **full** matrix —
storms and first screen alike.

That needed an upstream fix. Until
[octanejs/octane#459](https://github.com/octanejs/octane/pull/459), Octane
received no taps at all on Lynx for Web and its background thread never
committed: the two Lynx threads are separate JS realms on web (the background
runs in an iframe), and Octane's validators used a realm-local
`getPrototypeOf(v) === Object.prototype` check, so main rejected every message
the background sent. **Octane figures published here before that fix measured
main-thread-only first-screen paint and are superseded.**

The result is shape-dependent rather than a flat penalty. Octane is an order of
magnitude behind on **point** updates (`select@10k` 13× `vdom`, 22× `vapor`),
~2× on create, a flat ~1.8× on the mount-create ladder — and at 30k **batch**
updates it is *faster* than `vdom` (0.86×). Its per-node object command
protocol re-ships whole-node commands through structured clone on every update,
with no equivalent of the tiny op payload the Vue cells collapse a point update
into; under batch load that per-node cost amortizes and the gap closes.

Method, caveats and the full reading:
[`packages/benchmark/OCTANE.md`](https://github.com/Huxpro/vue-lynx/blob/vapor/packages/benchmark/OCTANE.md).

## Environment rules

- **lynx-web** — primary product scale (this report).
- **bare-dom** / **node-jitless** — different instruments; never ratio across them.

## Reproduce

```bash
pnpm --filter vue-lynx-benchmark bench:unified
pnpm --filter vue-lynx-benchmark bench:synthesize
pnpm --filter vue-lynx-benchmark bench:report
```

Design notes: [`packages/benchmark/UNIFIED.md`](https://github.com/Huxpro/vue-lynx/blob/vapor/packages/benchmark/UNIFIED.md) ·
raw analysis: [`ANALYSIS.md`](https://github.com/Huxpro/vue-lynx/blob/vapor/packages/benchmark/results/unified/ANALYSIS.md).
