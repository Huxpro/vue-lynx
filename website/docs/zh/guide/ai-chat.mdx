# AI Chat

一个功能完整的 AI 聊天应用，逐特性移植自官方 [Nuxt AI Chatbot 模板](https://github.com/nuxt-ui-templates/chat)（Nuxt UI + Vercel AI SDK）——这可能是迄今对 Vue Lynx 要求最高的「真实世界」验证：流式响应与思考/推理过程、带代码高亮和表格的 Markdown、工具调用卡片（天气、图表、网络搜索来源）、按日期分组的聊天历史、点赞/点踩、消息编辑、分享/重命名/删除、模型选择器、可持久化的明暗模式运行时主题、附件、模糊命令面板搜索，以及带侧滑抽屉的响应式移动端布局。

试试下面的快捷提示——*"What is the weather in Bordeaux?"* 会流式输出推理过程和天气卡片；*"Show me a chart of sales data"* 渲染 SVG 折线图；*"Help me create a Vue composable"* 展示带高亮代码的 Markdown。在 native 标签页扫描二维码即可在 [LynxExplorer](https://lynxjs.org/guide/start/quick-start.html) 中运行同一个 bundle。

<Go example="ai-chat" defaultFile="src/App.vue" />

上面的演练场完全自包含：应用会探测 API 服务器，不可达时自动回退到内置演示后端（相同的种子历史与确定性 mock AI 流）。在本地用 `pnpm dev:server` 运行示例时，同一套 UI 会连接一个复刻了 Nuxt 模板 API 路由的独立 Node 服务器——设置 `AI_GATEWAY_API_KEY` 后还可通过 [Vercel AI Gateway](https://vercel.com/docs/ai-gateway) 流式调用**真实模型**。

## 原生聊天的关键技巧

一次流畅的消息发送需要协调布局、滚动、键盘状态和动画。[Vercel 的 v0 iOS 技术复盘](https://vercel.com/blog/how-we-built-the-v0-ios-app)启发了这套写法：先定义用户能感知的交互目标，再解释各个子系统如何共同实现它。Vue Lynx 版本把这个方法落在四个技巧上。

### 让输入框跟随键盘

Lynx 的 [`<input>` 元素不会自动避让键盘](https://lynxjs.org/zh/api/elements/built-in/input#keyboard-avoidance)。输入框保持绝对定位在底部，并监听 [`keyboardstatuschanged` 全局事件](https://lynxjs.org/zh/api/lynx-api/event/global-event#keyboardstatuschanged)。应用通过 `setNativeProps` 使用事件报告的键盘高度；消息列表只在原本跟随新内容时继续滚动。

### 为不同渲染器选择滚动策略

原生端发送消息后，会把新的用户轮次定位到顶部。Vue Lynx 的 [`nextTick()`](https://vue.lynxjs.org/zh/guide/api/vue-lynx/Function.nextTick)先等待待处理操作到达主线程，再用 [`scrollIntoView`](https://lynxjs.org/zh/api/elements/built-in/scroll-view#scrollintoview)对齐该轮次。Web 端则更新可观察的 `scroll-top`，在发送和流式输出期间保持列表位于底部。

### 在布局稳定后启动发送动画

气泡高度、视口、输入框和键盘共同决定气泡的起始位移。应用先把气泡暂存在输入框位置，提交锚点和滚动位置，再在后续帧启动过渡。助手消息会等待这次交接完成，从而避免目标位置闪现或短暂露出上一轮消息。

### 把手势反馈留在主线程

抽屉手势和按压反馈使用 Vue Lynx 的[主线程脚本](https://vue.lynxjs.org/zh/guide/main-thread-script)。带有 `'main thread'` 标记的函数直接响应 `main-thread-bindtouch*` 事件并更新元素样式，无需往返后台线程。普通的 `tap` 处理器仍在 Vue 后台线程中执行应用操作。

## 移植笔记

移植保留了原版的数据模型（AI SDK v5 `UIMessage` parts + UI-message-stream 协议）和几乎全部 composable/页面逻辑，同时用 Lynx 元素重写了与 DOM 耦合的层：

| 方面 | Nuxt 原版 | Vue Lynx |
|------|-----------|----------|
| **UI 组件库** | @nuxt/ui v4（`UChat*`、`UDashboard*`、Reka UI portal） | 手写 Lynx 组件（`view`/`text`/`image`/`scroll-view`/`input`），复用 Nuxt UI 的语义设计令牌 |
| **聊天状态** | `@ai-sdk/vue` `useChat` | 自研 `useChat`，复用同一套协议（Web 走 SSE；native 走增量轮询并支持取消） |
| **Markdown** | Comark + Shiki（HTML 输出） | Markdown（含表格）→ 原生节点渲染器 + 轻量代码分词器 |
| **图表** | nuxt-charts / Unovis（SVG DOM） | 生成内联 SVG 字符串交给原生 `<svg>` 元素 |
| **下拉/弹窗** | 锚定 popover、portal | 根节点的 action sheet + 居中弹窗 |
| **主题** | Tailwind v4 + `app.config` | 运行时 `--ui-*` CSS 变量（含 17 主色 × 5 中性色选择器） |
| **响应式** | Tailwind `lg:` 断点 | `SystemInfo.pixelWidth / pixelRatio` 分支（Lynx 无媒体查询） |

示例目录包含完整的[逐特性对照 PRD](https://github.com/huxpro/vue-lynx/blob/main/examples/ai-chat/PRD.md)（65 项移植/适配，10 项注明原因跳过）、记录复用与重写决策及平台坑点的 [PORTING.md](https://github.com/huxpro/vue-lynx/blob/main/examples/ai-chat/PORTING.md)，以及在 Lynx for Web 上与原版实况对拍的[逐屏截图对比](https://github.com/huxpro/vue-lynx/blob/main/examples/ai-chat/screenshots/COMPARISON.md)。
