{
  "skip_list": [
    "optimized props updates",
    "validator should be called with two arguments",
    "validator should not be able to mutate other props",
    "events should not be props when component updating",
    "config.throwUnhandledErrorInProduction",

    "isEmitListener",

    "basic usage",
    "should insert jobs in ascending order of job's id when flushing",
    "should dedupe queued jobs",
    "queueJob while flushing",
    "queueJob inside preFlushCb",
    "queueJob & preFlushCb inside preFlushCb",
    "should insert jobs after pre jobs with the same id",
    "preFlushCb inside queueJob",
    "should insert pre jobs without ids first during flushing",
    "queue preFlushCb inside postFlushCb",
    "queueJob inside postFlushCb",
    "queueJob & postFlushCb inside postFlushCb",
    "postFlushCb inside queueJob",
    "queueJob & postFlushCb inside queueJob",
    "nested queueJob w/ postFlushCb",
    "jobs added during post flush are ordered correctly",
    "sort job based on id",
    "sort SchedulerCbs based on id",
    "avoid duplicate postFlushCb invocation",
    "nextTick should capture scheduler flush errors",
    "jobs can be re-queued after an error",
    "should prevent self-triggering jobs by default",
    "should allow explicitly marked jobs to trigger itself",
    "recursive jobs can only be queued once non-recursively",
    "recursive jobs can only be queued once recursively",
    "recursive jobs can't be re-queued by other jobs",
    "jobs are de-duplicated correctly when calling flushPreFlushCbs",
    "flushPostFlushCbs",
    "should not run stopped reactive effects",
    "flushPreFlushCbs inside a pre job",
    "flushPreFlushCbs inside a post job",
    "nextTick should return promise",
    "nested flushPostFlushCbs",


    "handle error in watch & watchEffect",
    "should pause tracking in error handler",
    "errors in scheduler job with owner instance should be caught",
    "handle error in computed",

    "initSlots: should normalize object slots (when value is null, string, array)",
    "initSlots: should normalize object slots (when value is function)",
    "initSlots: instance.slots should be set correctly (when vnode.shapeFlag is not SLOTS_CHILDREN)",
    "updateSlots: instance.slots should be updated correctly (when slotType is null)",
    "updateSlots: instance.slots should be update correctly (when vnode.shapeFlag is not SLOTS_CHILDREN)",



    "should work",
    "should work with a function directive",
    "should work on component vnode",

    "normalizeVNode",
    "cloneVNode ref normalization",
    "cloneVNode ref merging",
    "error of slot function execution should not affect block tracking",


    "stopping the watcher (SSR)",




    "should release property Dep instance if it no longer has subscribers",
    "should remove the dep when the effect is stopped",
    "should only remove the dep when the last effect is stopped",

    "test with higher level APIs",
    "events: onTrack",
    "debug: the call sequence of onTrack",
    "debug: the call sequence of onTrigger",
    "should be triggered once with batching",
    "nested effect should force track in untracked zone",
    "should remove the dep when it is no longer used by the effect",
    "should get called correctly",

    "$watch should always register its effects with its own instance",
    "should not track openBlock() when tracking is disabled",
    "toRef",
    "attempting to write plain value to a readonly ref nested in a reactive object should fail",
    "can not run an inactive scope",

    "handle error in async onActivated",

    "should work with SVG",
    "should work with block tree",
    "toggle sibling node inside target node",
    "unmount previous sibling node inside target node",
    "should be able to target content appearing later than the teleport with defer",
    "defer mode should work inside suspense"
  ],
  "_categories": {
    "scheduler": "Tests internal scheduler API directly, not renderer contract",
    "directives": "el binding in directives gets TestNode wrapper instead of raw element",
    "vnode": "Tests internal VNode normalization/cloning, not renderer",
    "error_handling": "Internal error handling mechanisms",
    "component_slots": "Internal slot normalization, uses ShapeFlags",
    "api_watch": "SSR test, internal flush timing",
    "componentEmits": "Internal emit listener check",
    "apiInject": "runWithContext internal API",
    "componentProps": "Optimized mode, validator internals",
    "reactivity_internals": "Uses targetMap/getDepFromReactive — not in public @vue/reactivity API",
    "reactivity_version_mismatch": "Tests from v3.5.12 source, installed vue is v3.5.30 — internal class names changed (ObjectRefImpl vs RefImpl), readonly/effectScope behavior differs",
    "apiExpose_ref_owner": "Adapter createApp().render() doesn't set proper ref owner context for template refs on exposed instances",
    "apiAsyncComponent_ref_forwarding": "Template ref forwarding on async wrapper components not supported by adapter",
    "scopeId_suspense": "Suspense content scopeId attachment requires internal Suspense implementation detail",
    "keepAlive_async_onActivated": "Adapter's TestElement wrapper triggers Vue warning about enumerating component instance keys; unrelated to KeepAlive logic",
    "teleport_dom_renderer": "Tests use domRender/createDOMApp (DOM renderer), our adapter only provides the test renderer — parent._se is undefined on HTMLElement",
    "teleport_compile": "Tests use compile() (runtime template compiler), not available in this build"
  }
}
