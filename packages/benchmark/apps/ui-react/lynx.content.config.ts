import { pluginReactLynx } from '@lynx-js/react-rsbuild-plugin';
import { defineConfig } from '@lynx-js/rspeedy';

// CONTENT variant: first-screen FCP scaling. src/ContentApp.tsx +
// src/index-content.tsx are generated by core/workloads/content-react.mjs.
// Builds to dist-content. ReactLynx ships snapshot + IFR always-on.
export default defineConfig({
  environments: {
    web: {},
    lynx: {},
  },
  source: {
    entry: {
      main: './src/index-content.tsx',
    },
  },
  output: {
    distPath: {
      root: 'dist-content',
    },
  },
  plugins: [pluginReactLynx()],
});
